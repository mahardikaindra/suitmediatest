import * as LoginAction from "../collections/Login/actions";
import * as UsersAction from "../collections/Users/actions";

export const ActionCreators = Object.assign(
  LoginAction,
  UsersAction,
);
